/* MedForward - 08-15-2019;*/

var scene = new THREE.Scene(), camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, .1, 1e3), renderer = new THREE.WebGLRenderer();

renderer.setSize(window.innerWidth, window.innerHeight), document.body.appendChild(renderer.domElement);

var geometry = new THREE.BoxGeometry(1, 1, 1), material = new THREE.MeshBasicMaterial({
    color: 15790320
}), cube = new THREE.Mesh(geometry, material);

scene.add(cube), camera.position.z = 5;

var animate = function() {
    if (WEBGL.isWebGLAvailable()) animate(); else {
        var e = WEBGL.getWebGLErrorMessage();
        document.getElementById("container").appendChild(e);
    }
    requestAnimationFrame(animate), cube.rotation.x += .01, cube.rotation.y += .01, 
    renderer.render(scene, camera);
};

animate();
//# sourceMappingURL=customThree.min.js.map